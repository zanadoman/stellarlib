i/stellarlib.hpp
s/stellarlib.cpp
t/stellarlib.cpp
i/utility.hpp
t/utility.cpp
i/functional.hpp
t/functional.cpp
i/any_set.hpp

---------------------------

clang format off
license
header guard
include
std
spaces (19)
member init
=
& * { ->
constexpr
nodiscard
++ -- += -= *= /= %=
!= true false
return
else
const auto
const correction
<>

---------------------------

SparseStorage<Component> components;

HashMap<Entity, ArchetypeId> entities;
Vec<(Archetype, HashSet<Entity>)> archetypes;

HashMap<TypeId, (Archetype, Vec<ArhetypeId>)> queries;
